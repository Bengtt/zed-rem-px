[package]
name = "cssrem"
version = "0.1.0"
edition = "2021"
publish = false

[lib]
crate-type = ["cdylib"]

[dependencies]
zed_extension_api = "0.7"

# Prevent Cargo from treating lsp/ as a workspace member.
[workspace]
